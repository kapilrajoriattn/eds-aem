version: 1
indices:
  default:
    include:
      - /**
    target: /query-index.json
    properties:
      customProperty:
        select: head > meta[name="custom-property"]
        value: attribute(el, "content")
      
        
